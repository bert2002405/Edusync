// Use the Wi-Fi IP address for both development and production
const baseURL = 'http://192.168.254.128:5000/api';
export const API_URL = baseURL;

// Export the base URL for other API endpoints if needed
export const BASE_URL = baseURL;
